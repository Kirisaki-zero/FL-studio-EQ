# Implementasi Tauri & Audio Engine

- [ ] 1. Restrukturisasi direktori (Pindahkan `src` dan `Cargo.toml` ke `src-tauri`, ubah `fl_eq_v3.html` jadi `index.html`).
- [ ] 2. Inisialisasi frontend Node.js (`package.json`, Vite, atau polosan).
- [ ] 3. Inisialisasi dan konfigurasi proyek Tauri (`tauri.conf.json`, `Cargo.toml` dependencies).
- [ ] 4. Modifikasi `main.rs` untuk menjalankan aplikasi Tauri (Tauri Builder).
- [ ] 5. Implementasi *Audio Engine* di Rust:
    - [ ] Setup `cpal` stream dan *thread* khusus pemutaran audio.
    - [ ] Integrasi `claxon` untuk membaca *buffer* file FLAC secara *real-time*.
    - [ ] Implementasi filter **7-Band EQ** (menggunakan *crate* `biquad`).
- [ ] 6. Setup Komunikasi IPC (Inter-Process Communication):
    - [ ] Tauri Commands untuk memulai audio (`play_audio`).
    - [ ] Tauri Commands untuk mengubah gain EQ secara seketika (`set_eq_gain`).
- [ ] 7. Modifikasi antarmuka (`index.html` / `main.js`) agar memanggil fungsi Rust saat *slider* EQ digeser.
- [ ] 8. Pengujian dan Verifikasi.
